<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="robots" content="noindex,nofollow,noarchive">
<title>体检二次判读 · 改版方案</title>
<style>
:root{
  --ink:#1B2127; --ink-2:#3A444C; --muted:#6B757D;
  --paper:#EDEFF0; --card:#FFF; --rule:#D2D8DC; --rule-2:#E4E8EB;
  --act:#A8452A; --act-bg:#F7EDE9;
  --clarify:#3A5F8A; --clarify-bg:#EAF0F7;
  --settle:#1F5E56; --settle-bg:#E7F0EE;
  --grey:#6B757D; --band:#D8DEE2;
  --serif:"Songti SC","Source Han Serif SC","Noto Serif CJK SC",Georgia,serif;
  --sans:"PingFang SC","Hiragino Sans GB","Source Han Sans SC","Microsoft YaHei",system-ui,sans-serif;
  --mono:ui-monospace,"SF Mono",Menlo,Consolas,monospace;
}
*{box-sizing:border-box}
body{margin:0;background:var(--paper);color:var(--ink);font-family:var(--sans);
  font-size:15px;line-height:1.75;-webkit-font-smoothing:antialiased}
.wrap{max-width:920px;margin:0 auto;padding:0 22px 90px}
a{color:inherit}
.eyebrow{font-family:var(--mono);font-size:11px;letter-spacing:.14em;text-transform:uppercase;
  color:var(--muted);margin:0 0 20px}
h2.sec{font-family:var(--serif);font-size:23px;font-weight:600;margin:56px 0 6px;
  scroll-margin-top:16px}
.sub{color:var(--muted);font-size:13.5px;margin:0 0 18px;max-width:66ch}

/* ---- 首屏 ---- */
.hero{padding:52px 0 34px;border-bottom:1px solid var(--rule)}
.idline{font-family:var(--mono);font-size:11.5px;letter-spacing:.06em;color:var(--muted);margin:0 0 22px}
.thesis{font-family:var(--serif);font-size:clamp(25px,4vw,38px);line-height:1.38;font-weight:600;
  margin:0 0 18px;text-wrap:balance}
.thesis em{font-style:normal;color:var(--act);border-bottom:2px solid var(--act)}
.hero p{max-width:64ch;color:var(--ink-2);margin:0 0 12px}

/* ---- 色带总览 ---- */
.bandbar{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin:30px 0 0}
.bandcard{background:var(--card);border:1px solid var(--rule);border-radius:5px;
  padding:16px 18px;border-top:4px solid;text-decoration:none;display:block}
.bandcard:hover{border-color:var(--ink-2)}
.bandcard b{font-family:var(--mono);font-size:30px;line-height:1;display:block;margin-bottom:6px}
.bandcard .lbl{font-size:15px;font-weight:600;display:block;margin-bottom:3px}
.bandcard .d{font-size:12.5px;color:var(--muted);line-height:1.5}
.chip-note{margin:14px 0 0;font-size:13px;color:var(--muted)}
.chip{display:inline-block;font-family:var(--mono);font-size:10.5px;letter-spacing:.06em;
  padding:2px 7px;border-radius:2px;background:#E6EAEC;color:var(--ink-2);white-space:nowrap}
.chip.confirm{background:#F3EEE4;color:#7A5A18}

/* ---- 对账表 ---- */
.recon{border-top:1px solid var(--rule);margin-top:8px}
.rrow{display:grid;grid-template-columns:26px minmax(0,1.15fr) minmax(0,1.35fr);gap:16px;
  padding:14px 4px;border-bottom:1px solid var(--rule-2);align-items:start}
.rrow:hover{background:var(--card)}
.dot{width:9px;height:9px;border-radius:50%;margin-top:8px}
.rrow .said{font-size:13.5px;line-height:1.6}
.rrow .said .src{font-family:var(--mono);font-size:10.5px;color:var(--muted);display:block;
  letter-spacing:.04em;margin-bottom:2px}
.rrow .said .adv{display:block;color:var(--muted);font-size:12.5px;margin-top:3px}
.rrow .ours{font-size:13.5px;line-height:1.62}
.rrow .ours a{font-family:var(--mono);font-size:11px;color:var(--muted);
  text-decoration:none;border-bottom:1px solid var(--rule);white-space:nowrap}
.rrow .ours a:hover{color:var(--ink);border-color:var(--ink)}
.gap{grid-column:1/-1;font-family:var(--mono);font-size:10.5px;letter-spacing:.1em;
  text-transform:uppercase;color:var(--muted);padding:16px 0 2px;border-bottom:0}

/* ---- 章节 ---- */
.section-band{margin:64px 0 16px;padding:14px 0 12px;border-top:3px solid;border-bottom:1px solid var(--rule)}
.section-band h2{font-family:var(--serif);font-size:24px;margin:0 0 3px;font-weight:600}
.section-band p{margin:0;color:var(--muted);font-size:13.5px}
.section-band .cnt{font-family:var(--mono);font-size:12px;color:var(--muted);float:right;margin-top:8px}

.chain{background:var(--card);border:1px solid var(--rule);border-left:4px solid;
  border-radius:5px;margin:16px 0;padding:24px 26px;scroll-margin-top:16px}
.chain .title{font-family:var(--serif);font-size:20px;line-height:1.42;margin:0 0 8px;font-weight:600}
.chain .lede{color:var(--ink-2);margin:0 0 18px;max-width:70ch;font-size:14px}
.cap{font-family:var(--mono);font-size:10.5px;letter-spacing:.11em;text-transform:uppercase;
  color:var(--muted);margin:0 0 8px}

.inputs{border-top:1px solid var(--rule-2);padding-top:14px;margin-bottom:20px}
.m{display:grid;grid-template-columns:minmax(0,1.5fr) 104px 120px 92px;gap:12px;align-items:center;
  padding:6px 0;border-bottom:1px solid #F1F4F5}
.m:last-child{border-bottom:0}
.m-name{font-size:13px;line-height:1.42}
.m-name small{display:block;color:var(--muted);font-size:11px}
.m-val{font-family:var(--mono);font-size:13.5px;text-align:right;font-variant-numeric:tabular-nums}
.m-val .u{font-size:10px;color:var(--muted);margin-left:3px}
.m.hi .m-val{color:var(--act);font-weight:600}
.m.lo .m-val{color:var(--settle);font-weight:600}
.track{position:relative;height:15px}
.track .line{position:absolute;top:7px;left:0;right:0;height:1px;background:var(--rule)}
.track .bandx{position:absolute;top:4.5px;height:6px;background:var(--band);border-radius:1px}
.track .pin{position:absolute;top:1.5px;width:2px;height:12px;background:var(--ink)}
.m.hi .pin{background:var(--act)} .m.lo .pin{background:var(--settle)}
.track.none{opacity:.3}
.m-ref{font-family:var(--mono);font-size:10.5px;color:var(--muted);text-align:right}

.reading{display:grid;grid-template-columns:1fr 1fr;border:1px solid var(--rule-2);
  border-radius:4px;overflow:hidden;margin-bottom:18px}
.reading>div{padding:16px 18px}
.reading .single{background:#F7F8F9;border-right:1px solid var(--rule-2)}
.reading h4{font-family:var(--mono);font-size:10px;letter-spacing:.1em;text-transform:uppercase;
  color:var(--muted);margin:0 0 7px;font-weight:500}
.reading p{margin:0;font-size:13.6px;line-height:1.7}
.reading .single p{color:var(--ink-2)}

.verdict{display:grid;grid-template-columns:1fr 1fr;gap:0 24px;padding:16px 0 0;
  border-top:1px solid var(--rule-2)}
.verdict>div{padding:5px 0}
.verdict h4{font-family:var(--mono);font-size:10px;letter-spacing:.1em;text-transform:uppercase;
  margin:0 0 6px;font-weight:500;color:var(--muted)}
.verdict .yes h4{color:var(--settle)} .verdict .no h4{color:var(--act)}
.verdict p,.verdict ul{margin:0;font-size:13.4px;line-height:1.68}
.verdict ul{padding-left:16px} .verdict li{margin-bottom:3px}
.cmp{font-size:12.6px;line-height:1.6;padding:6px 0;border-bottom:1px dotted var(--rule-2)}
.cmp:last-child{border-bottom:0}
.cmp b{font-family:var(--mono);font-size:11.5px}
.cmp .cav{display:block;color:var(--act);font-size:11px}

/* ---- 折叠 ---- */
details{background:var(--card);border:1px solid var(--rule);border-radius:5px;margin:12px 0}
details[open]{border-color:var(--rule)}
summary{padding:15px 22px;cursor:pointer;list-style:none;display:grid;
  grid-template-columns:minmax(0,1fr) auto;gap:14px;align-items:baseline}
summary::-webkit-details-marker{display:none}
summary .st{font-size:15px;font-weight:600;line-height:1.5}
summary .sd{font-size:13px;color:var(--ink-2);line-height:1.6;margin-top:3px;font-weight:400}
summary .hint{font-family:var(--mono);font-size:10.5px;color:var(--muted);white-space:nowrap}
details .inner{padding:2px 22px 20px;border-top:1px solid var(--rule-2)}
details.settle{border-left:4px solid var(--settle)}
details.confirm{border-left:4px solid #A98432}

.dl{display:flex;flex-wrap:wrap;gap:6px;margin:12px 0 14px}
.dl span{font-family:var(--mono);font-size:11px;background:#F0F3F4;padding:3px 8px;
  border-radius:2px;color:var(--ink-2)}
.dl span.flag{background:var(--act-bg);color:var(--act);font-weight:600}
.orig{background:#F7F8F9;border-left:2px solid var(--rule);padding:9px 13px;margin:0 0 12px;
  font-size:12.8px;color:var(--ink-2)}
.orig b{font-family:var(--mono);font-size:10.5px;letter-spacing:.07em;color:var(--muted);
  display:block;text-transform:uppercase;margin-bottom:2px}
.two{display:grid;grid-template-columns:1.3fr 1fr;gap:24px}
.two h5{font-family:var(--mono);font-size:10px;letter-spacing:.1em;text-transform:uppercase;
  color:var(--muted);margin:0 0 6px;font-weight:500}
.two p{margin:0;font-size:13.4px;line-height:1.7}

table{width:100%;border-collapse:collapse;font-size:13px;background:var(--card);
  border:1px solid var(--rule);border-radius:5px;overflow:hidden}
th{text-align:left;font-family:var(--mono);font-size:10px;letter-spacing:.1em;text-transform:uppercase;
  color:var(--muted);font-weight:500;padding:10px 13px;border-bottom:1px solid var(--rule);background:#F7F8F9}
td{padding:9px 13px;border-bottom:1px solid var(--rule-2);vertical-align:top;line-height:1.58}
tr:last-child td{border-bottom:0}
td.num{font-family:var(--mono);font-variant-numeric:tabular-nums;white-space:nowrap}
.note{font-size:13px;color:var(--ink-2);line-height:1.8;margin:12px 0 0}
.note b{color:var(--ink)}
footer{margin-top:60px;padding-top:24px;border-top:1px solid var(--rule);
  font-size:12.8px;color:var(--ink-2);line-height:1.8}
@media (max-width:760px){
  .bandbar{grid-template-columns:1fr}
  .rrow{grid-template-columns:18px 1fr;gap:10px}
  .rrow .ours{grid-column:2}
  .m{grid-template-columns:1fr 88px} .track,.m-ref{display:none}
  .reading,.verdict,.two{grid-template-columns:1fr}
  .reading .single{border-right:0;border-bottom:1px solid var(--rule-2)}
}
@media print{
  body{background:#fff} details{break-inside:avoid} details:not([open]) .inner{display:none}
  .chain{break-inside:avoid}
}
</style>
</head>
<body><div class="wrap">

<header class="hero">
  <p class="idline">{{ subject_id }} · {{ sex }} · {{ age }} 岁 · 健检 {{ period }}</p>
  {% if n_orig %}
  <h1 class="thesis">原报告给了 {{ n_orig }} 条结论。<br>
    按组合重读之后，真正需要你现在动手的是 <em>{{ bands[0].n }} 件</em>。</h1>
  <p>下面第一张表把原报告的每一条都接住了——它说了什么、我们的处理是什么，一行一条。
     这张表同时也是目录，点进去才是详情。</p>
  {% else %}
  <h1 class="thesis">本次 {{ n_obs }} 条观察、{{ n_abnormal }} 项异常。<br>
    按组合重读之后，真正需要你现在动手的是 <em>{{ bands[0].n }} 件</em>。</h1>
  <p>本次未取得原报告的结论清单，因此下表按本判读自身的结论组织；
     一旦补上原报告结论，这里会改成逐条对账。</p>
  {% endif %}
  <p>颜色只有三种，含义固定：<b>要做</b>、<b>先弄清楚</b>、<b>可以放下</b>。
     全文不再出现别的分类。</p>

  <div class="bandbar">
  {% for b in bands %}
    <a class="bandcard" href="#s-{{ b.key }}" style="border-top-color:{{ b.color }};background:{{ b.bg }}">
      <b style="color:{{ b.color }}">{{ b.n }}</b>
      <span class="lbl" style="color:{{ b.color }}">{{ b.label }}</span>
      <span class="d">{{ b.desc }}</span>
    </a>
  {% endfor %}
  </div>
  <p class="chip-note">另有 <span class="chip confirm">◇ 与医生确认</span> 标记 {{ confirms|length }} 条——
     它不是第四种颜色，是叠加在上面三种之上的一个提醒：这几条原建议按组合看有疑问，
     执行前值得和医生确认一句。理由折叠在文末，不必先读。</p>
</header>

{% if n_orig %}
<h2 class="sec" id="recon">原报告 {{ n_orig }} 条，逐条对上</h2>
<p class="sub">左边是原报告的原话，右边是本次判读的处理。没有一条被略过，也没有一条被重复讲两遍。</p>
{% else %}
<h2 class="sec" id="recon">本次结论一览</h2>
<p class="sub">每条一行。点进去才是详情。</p>
{% endif %}
<div class="recon">
{% for b in bands %}
  {% if b.rows %}
  <div class="rrow" style="border-bottom:0;padding-bottom:0">
    <div class="gap" style="color:{{ b.color }}">{{ b.label }}</div>
  </div>
  {% for r in b.rows %}
  <div class="rrow">
    <div><div class="dot" style="background:{{ b.color }}"></div></div>
    <div class="said">
      {% if r.findings %}
        {% for f in r.findings %}
        <span class="src">{{ f.seq }} · {{ f.source }}</span>
        {{ f.said }}
        {% if f.advised %}<span class="adv">原建议：{{ f.advised }}</span>{% endif %}
        {% if not loop.last %}<div style="height:8px"></div>{% endif %}
        {% endfor %}
      {% else %}
        <span class="src">原报告未提及</span>
        本次判读新增的一条
      {% endif %}
    </div>
    <div class="ours">{{ r.headline }}
      {% if r.needs_confirm %}<span class="chip confirm">◇ 与医生确认</span>{% endif %}
      <br><a href="#{{ r.anchor }}">详见 →</a></div>
  </div>
  {% endfor %}
  {% endif %}
{% endfor %}
</div>

{% for b in bands %}
<div class="section-band" id="s-{{ b.key }}" style="border-top-color:{{ b.color }}">
  <span class="cnt">{{ b.chains|length }} 条</span>
  <h2 style="color:{{ b.color }}">{{ b.label }}</h2>
  <p>{{ b.desc }}</p>
</div>

{% for g in b.chains %}
{% set c = g.c %}
{% if b.key == 'settle' %}
<details class="settle" id="{{ c.id }}">
  <summary>
    <span><span class="st">{{ c.title }}</span>
      <span class="sd">{{ c.verdict.certain }}</span></span>
    <span class="hint">展开依据 · {{ g.rows|length }} 项数值</span>
  </summary>
  <div class="inner">
{% else %}
<section class="chain" id="{{ c.id }}" style="border-left-color:{{ b.color }}">
  <h3 class="title">{{ c.title }}</h3>
  {% if c.lede %}<p class="lede">{{ c.lede }}</p>{% endif %}
{% endif %}

  <div class="inputs">
    <p class="cap">进入判断的数值 · {{ g.rows|length }} 项</p>
    {% for r in g.rows %}
    <div class="m {{ r.state }}">
      <div class="m-name">{{ r.o.raw_name }}
        {% if r.note %}<small>{{ r.note }}</small>
        {% elif r.o.formula %}<small>{{ r.o.formula }}</small>{% endif %}</div>
      <div class="m-val">{{ r.o|disp }}{% if r.o.unit %}<span class="u">{{ r.o.unit }}</span>{% endif %}</div>
      <div class="track {% if not r.bar %}none{% endif %}"><div class="line"></div>
        {% if r.bar %}<div class="bandx" style="left:{{ r.bar.lo }}%;width:{{ r.bar.hi - r.bar.lo }}%"></div>
        <div class="pin" style="left:{{ r.bar.val }}%"></div>{% endif %}</div>
      <div class="m-ref">{{ r.ref }}</div>
    </div>
    {% endfor %}
  </div>

  <div class="reading">
    <div class="single"><h4>只看单项会得到</h4><p>{{ c.single_read }}</p></div>
    <div class="joint"><h4>放在一起看得到</h4><p>{{ c.joint_read }}</p></div>
  </div>

  {% if c.verdict %}
  <div class="verdict">
    <div class="yes"><h4>可以确定</h4><p>{{ c.verdict.certain }}</p></div>
    <div class="no"><h4>不能由此认定</h4><p>{{ c.verdict.not_certain }}</p></div>
    {% if c.verdict.next_steps %}
    <div><h4>下一步</h4><ul>{% for s in c.verdict.next_steps %}<li>{{ s }}</li>{% endfor %}</ul></div>{% endif %}
    {% if c.verdict.compare_next %}
    <div><h4>下次比什么</h4>{% for i in c.verdict.compare_next %}
      <div class="cmp"><b>{{ i.baseline_display }}</b> — {{ i.what }}
        {% if i.caveat %}<span class="cav">⚠ {{ i.caveat }}</span>{% endif %}</div>{% endfor %}</div>{% endif %}
  </div>
  {% endif %}

{% if b.key == 'settle' %}</div></details>{% else %}</section>{% endif %}
{% endfor %}
{% endfor %}

<h2 class="sec" id="confirm">这 {{ confirms|length }} 条，执行前和医生确认一句</h2>
<p class="sub">它们的共同点是：原报告确实标了箭头，但同一份报告里另有指标指向不同方向。
   我们不是接诊医生，所以这里给的不是"不用做"，而是"值得先问一句再做"。理由默认折叠。</p>
{% for x in confirms %}
<details class="confirm" id="{{ x.c.id }}">
  <summary>
    <span><span class="st">{{ x.c.claim_soft }}</span>
      <span class="sd">原建议：{{ x.recon.findings[0].advised if x.recon.findings else x.c.original_advice }}</span></span>
    <span class="hint">展开理由</span>
  </summary>
  <div class="inner">
    <div class="dl">
      <span class="flag">{{ x.flag.o.raw_name }} {{ x.flag.o|disp }}{{ x.flag.o.unit or '' }} ｜ {{ x.flag.ref }}</span>
      {% for r in x.rows %}<span>{{ r.o.raw_name }} {{ r.o|disp }}{{ r.o.unit or '' }}</span>{% endfor %}
    </div>
    <div class="two">
      <div><h5>同一份报告里指向不同方向的证据</h5><p>{{ x.c.why_not }}</p></div>
      <div><h5>建议怎么做</h5><p>{{ x.c.what_to_do }}</p>
        <p style="margin-top:10px;color:var(--muted);font-size:12.5px">这是按报告内部数据的组合判读，不是诊断。最终以接诊医生的判断为准。</p></div>
    </div>
  </div>
</details>
{% endfor %}

<h2 class="sec">还缺什么</h2>
<p class="sub">写清楚不知道什么，和写清楚知道什么同样重要。</p>
<table>
  <thead><tr><th style="width:34%">尚未纳入 / 尚不知道</th><th>影响什么</th></tr></thead>
  <tbody>
  {% for m in missing_context %}
    <tr><td>{{ m.label }} <span class="chip">背景空白</span></td>
      <td>报告里没有这项，本判读不替你假设。影响：{% for t in m.titles %}「{{ t }}」{% endfor %}</td></tr>
  {% endfor %}
  {% for b in boundaries %}
    <tr><td>{{ b.what }}{% if b.critical %} <span class="chip">最关键</span>{% endif %}</td>
      <td>{{ b.impact }}</td></tr>
  {% endfor %}
  </tbody>
</table>

<details style="margin-top:28px">
  <summary><span><span class="st">原报告没算、但这批数据够算的 {{ derived|length }} 个指标</span>
    <span class="sd">不是新检查，全部由已有数值算出。上面几条主要判读依赖的正是这些数。</span></span>
    <span class="hint">展开</span></summary>
  <div class="inner"><table style="border:0">
    <thead><tr><th>指标</th><th>结果</th><th>算法</th><th>改变了什么判断</th></tr></thead>
    <tbody>{% for d in derived %}
      <tr><td>{{ d.o.raw_name }}</td><td class="num">{{ d.o|disp }} {{ d.o.unit or '' }}</td>
        <td class="num">{{ d.o.formula }}</td><td>{{ d.note or '—' }}</td></tr>
    {% endfor %}</tbody></table></div>
</details>

<details>
  <summary><span><span class="st">已经确认没有问题的部分</span>
    <span class="sd">{{ n_ok }} 项。这一段存在的意义只有一个：确认它们被看过了，而不是被略过了。</span></span>
    <span class="hint">展开</span></summary>
  <div class="inner">
  {% for src, items in by_src.items() %}
    <p class="note"><b>{{ src }}</b>（{{ items|length }}）　
      {% for o in items %}{{ o.raw_name }} {{ o|disp }}{{ o.unit or '' }}{% if not loop.last %}　·　{% endif %}{% endfor %}</p>
  {% endfor %}
  </div>
</details>

<details>
  <summary><span><span class="st">数据来源与覆盖率</span>
    <span class="sd">{{ sources|length }} 份报告合并；{{ n_obs }} 条观察每一条都有归宿，异常项若无归宿则本报告不会生成。</span></span>
    <span class="hint">展开</span></summary>
  <div class="inner">
    <table style="border:0;margin-bottom:14px">
      <thead><tr><th style="width:32%">来源</th><th style="width:24%">日期</th><th>合并核对</th></tr></thead>
      <tbody>{% for s in sources %}
        <tr><td>{{ s.label }}</td>
          <td class="num">{{ s.date_from }}{% if s.date_to and s.date_to != s.date_from %} 至 {{ s.date_to }}{% endif %}</td>
          <td>{{ s.merge_note or '—' }}</td></tr>
      {% endfor %}</tbody>
    </table>
    {% set total = coverage.records|length %}
    <table style="border:0">
      <thead><tr><th>归宿</th><th style="width:16%">条数</th></tr></thead>
      <tbody>{% for k, v in coverage.histogram().items()|sort(attribute='1', reverse=true) %}
        <tr><td>{{ k }}</td><td class="num">{{ v }}</td></tr>
      {% endfor %}</tbody>
    </table>
    {% if unjudged %}
    <p class="note"><b>暂未判定高低</b>　{{ unjudged|join('、') }}——原报告给了参考区间，
      但写法含条件从句，本系统没有把握读准，因此保留原值不判高低，也不据此安排任何干预。</p>
    {% endif %}
    {% if consistency %}
    <p class="note"><b>一致性校验</b>　{% for i in consistency %}{{ i.code }}：原报告 {{ i.reported }}，
      用原始值重算 {{ i.recomputed }}，相差 {{ "%.1f%%"|format(i.rel_diff * 100) }}。{% endfor %}
      不影响本次任何结论；记录下来是因为下次比较时要知道用的是哪一个口径。</p>
    {% endif %}
  </div>
</details>

<footer>
  <p><b>这份文件是什么：</b>对本次体检报告及 {{ sources|length - 1 }} 份附件的二次整理，方法是把彼此相关的指标放在一起判读，
    而不是逐项解释箭头。共 {{ n_obs }} 条观察、{{ n_abnormal }} 项异常，每一条都有归宿。</p>
  <p><b>这份文件不是什么：</b>不是诊断，不构成用药建议，也不替代经治医生的判断。
    标为「现在要做」和「与医生确认」的条目，请带原报告与医生当面核对。</p>
</footer>

</div></body></html>
