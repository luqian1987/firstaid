# 肿瘤 NGS 实验室落位方案 · 现场汇报材料

面向医院方的项目洽谈总结会汇报材料。与本仓库的体检报告判读系统无关，
仅借此分支存放交付物。

场地为病理科现有空闲 PCR 多分区实验室，六间房及各自缓冲间均已建成，
**不涉及新建与装修改造**——材料只讲功能分配与设备落位。

## 文件

两个版本，同一份数据：

| 文件 | 说明 |
| --- | --- |
| `ngs-lab.html` | **投屏版**：彩色分区流程图，深浅主题自适应 |
| `tpl3.html` / `build3.py` | 投屏版的模板与生成脚本 |
| `ngs-report-print.html` | **打印稿**：黑白 A4 竖版院内交流报告，宋体正文黑体标题、三线表、首行缩进 |
| `tpl_print.html` / `build_print.py` | 打印稿的模板与生成脚本 |

打印稿刻意只有一套白纸黑字的配色，不随观看者主题切换 —— 它是拿来打印的。
`build_print.py` 带两道守卫：CSS 变量必须定义，且样式表里不得出现非灰阶的
十六进制颜色。

## 重新生成

原始 xlsx 未入库（属客户资料）。将《设备配置清单》与《可开展检测项目与收费》
放回 两个脚本中 `UP` 指向的路径后：

```
pip install openpyxl
python3 build3.py      # 投屏版
python3 build_print.py # 打印稿
```

`build3.py` 内置断言：每间「另含 N 项」由清单条数减去已列出的主要设备算得，
不是手写；模板占位符与已删除的措辞（现场门牌、落位要点）也一并断言，
数据或文案对不上会直接报错，不会生成错页。

## 房间对照

分区流程图折成两行显示，现场实际是沿一条走廊的直线。功能分区与房间号的对应：

| 顺序 | 功能分区 | 房间 |
| --- | --- | --- |
| 1 | 试剂准备区 | 试剂准备间 |
| 2 | 标本与文库制备区 | PCR2 |
| 3 | 血浆文库制备区 | PCR3 |
| 4 | 杂交捕获区 | PCR4 |
| 5 | 文库扩增与检测区 | PCR5 |
| 6 | 测序区 | PCR8 |

PCR5 与 PCR8 之间还隔着 PCR6、PCR7 两间，本次不使用，流程图中以虚线标注。
